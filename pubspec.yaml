name: app_subscription_core
description: Shared subscription primitives for Flutter apps using flutter_inapp_purchase.
publish_to: "none"
version: 0.1.9

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_inapp_purchase: ^8.2.5
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
